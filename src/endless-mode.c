#include <stdio.h>
#include "tbendless.h"

int main(){
     titleScreen();

     //do you wish to start
          //if N, EXIT
          //if Y, continue
     
     //enter name
     //game begins

     //wave cycle (inf)
          //enemy cycle (5)
               //enemy subcycle
                    //if player death, exit to final score
                    //else, clean enemy data
          //boss subcycle
               //if player death, exit to final score
               //else, clean boss data

     //final score
     //leaderboard

     //game exit
     

     return 0;
}